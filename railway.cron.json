{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'cron service: no SvelteKit build needed'"
  },
  "deploy": {
    "startCommand": "npm run reminders:weekly",
    "cronSchedule": "0 9 * * 1",
    "restartPolicyType": "NEVER"
  }
}
